spring.datasource:
  url: jdbc:mysql://localhost/ReservedSystem?createDatabaseIfNotExist=true&serverTimezone=Europe/Prague
  username: root
  password: Superroot17.
  driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

springdoc:
  api-docs.path: /api-docs

spring:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true

  jackson:
    mapper:
      accept-case-insensitive-enums: true

# JWT nastavení
jwt:
  secret: A3aK+jrNs5YDFN3T8h2R7ZDmF9B0XBt2aG6wZPi0V3Q=  # Base64 pro: good_secure_base64_key_here!
  expiration: 3600000 # 1 hodina v milisekundách

